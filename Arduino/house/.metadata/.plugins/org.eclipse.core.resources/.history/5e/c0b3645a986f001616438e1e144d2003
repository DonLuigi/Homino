#include <Arduino.h>
#include <ButtonComponent.h>

ButtonComponent buttonOtroskaRoletaGor (38, 50, "otrRolGor");
ButtonComponent buttonOtroskaZaluzijaGor (39, 50, "otrZalGor");
ButtonComponent buttonOtroskaZaluzijaDol (40, 50, "otrZalDol");
ButtonComponent buttonOtroskaRoletaDol (41, 50, "otrRolDol");
ButtonComponent buttonGalerijaGor (42, 50, "galGor");
ButtonComponent buttonGalerijaDol (44, 50, "galDol");
ButtonComponent buttonSpalnicaGor (43, 50, "spalnicaGor");
ButtonComponent buttonSpalnicaDol (45, 50, "spalnicaDol");

ButtonComponent* buttons[] =
{ &buttonOtroskaRoletaGor, &buttonOtroskaZaluzijaGor, &buttonOtroskaZaluzijaDol, &buttonOtroskaRoletaDol, &buttonGalerijaDol, &buttonGalerijaGor, &buttonSpalnicaGor, &buttonSpalnicaDol, NULL };

void setup ()
{

    Serial.begin (115200);
    Serial.println ("BEGIN");

    for (int i = 0; i < 8 * 4; i++)
    {
        pinMode (i, OUTPUT);
        digitalWrite (i, HIGH);
    }
}

void loop ()
{
    for (int i = 0; buttons[i] != NULL; i++)
    {
        if (buttons[i]->onPress ())
        {
            Serial.print (millis () / 1000);
            Serial.print (" PRESSED2: ");
            Serial.println (buttons[i]->name);
        }
        if (buttons[i]->onRelease ())
        {
            Serial.print (millis () / 1000);
            Serial.print (" RELEASED: ");
            Serial.println (buttons[i]->name);
        }
    }
}

